<%= form_with(model: notification_group, local: true) do |form| %>
  <% if notification_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notification_group.errors.count, "error") %> prohibited this notification_group from being saved:</h2>

      <ul>
        <% notification_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
